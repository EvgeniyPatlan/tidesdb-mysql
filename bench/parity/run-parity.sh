#!/usr/bin/env bash
# Run the parity corpus on both SUTs, normalize, classify, emit matrix.md.
# STUB: implemented in the parity build step (after both SUT images exist).
set -euo pipefail
echo "parity runner not yet implemented -- see bench/parity/README.md" >&2
exit 2
